---
interface Props{
    title?: string;
    id: string;
}
const {title, id} = Astro.props;
---

<section id={id}>
    {title && (<h3 class="text-6xl font-space-mono font-bold">{title}</h3>)}
    <slot />
</section>
